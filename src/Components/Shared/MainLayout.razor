@using RealworldBlazorHtmx.App.ServiceClient
@inherits BaseComponent<MainLayout.Input>

<AppHeaderComponent Data="@(new AppHeaderComponent.Input(Data.IsAuthenticated, Data.ActivePath, Data.User))"></AppHeaderComponent>

@Data.Body

<AppFooterComponent/>

@code {

    public record Input(RenderFragment Body, bool IsAuthenticated, string ActivePath, User? User = null);

}